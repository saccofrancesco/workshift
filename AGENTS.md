## UI and Design

For any task involving UI, UX, styling, components, layout, responsive behavior, or animation, read and follow `DESIGN.md` before making changes.

`DESIGN.md` is the source of truth for the project's visual language, component architecture, motion system, interaction principles, accessibility requirements, and UI quality standards.

Do not introduce UI patterns that conflict with `DESIGN.md` unless explicitly requested.
